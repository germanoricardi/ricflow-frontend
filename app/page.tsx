import { Button, Typography } from "@mui/material";

export default function Home() {
  return (
    <div>
      <Typography>Olá! Bem-vindo(a) ao RicFlow</Typography>
      <Button variant="contained">Entrar</Button>
    </div>
  );
}
